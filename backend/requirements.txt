fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.22
httpx>=0.28.0
pydub>=0.25.1
anyio>=4.7.0
coqui-tts>=0.27.0
# XTTS Chinese pipeline (TTS tokenizer raises "Chinese requires: pypinyin" without this).
pypinyin>=0.50.0
# Coqui imports isin_mps_friendly; base PyTorch images ship an older transformers without it. 5.1+ removed it.
transformers>=4.57.0,<5.1
torch>=2.6.0
torchaudio>=2.6.0
